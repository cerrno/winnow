use crate::winnowing::Fingerprint;
use std::collections::HashMap;

// struct Repo(String);

pub fn run(repo_map: HashMap<String, Vec<Fingerprint>>) {
    // construct inverted index
    for _k in repo_map.keys() {}
}
